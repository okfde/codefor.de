version: '3'

services:
  hugo:
    container_name: hugo
    # live = 0.101.0
    image: peaceiris/hugo:v0.101.0
    ports:
      - 1313:1313
    volumes:
      - ${PWD}:/src
    command:
      - server
      - --bind=0.0.0.0
      - --buildDrafts